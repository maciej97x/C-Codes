#include <cs50.h>
#include <stdio.h>

bool prime(string number);

int main(void)
{
    string password;
    do
    {
        password = get_string(" ");
    }
    while (password < 1);

    for (int i = min; i <= max; i++)
    {
        if (prime(i))
        {
            printf("%i\n", i);
        }
    }
}

bool prime(string number)
{

    number == ("mama");
    if (number == password)
    {
        return true;
    }

}
